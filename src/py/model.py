import torch
import torch.nn as nn

class PortfolioOptimizerModel(nn.Module):

    def __init__(self):
        super(PortfolioOptimizerModel, self).__init__()

        # linear regression 
        # recurrent time-series layers
        # dropout for regularization
        # take that market data and output optimized weights



    def forward(self, x):

        # forward pass for model
        # process that data from the market data in PSQL or CSV (havent decided yet) as well as user preferenes to output asset alloc weights
        # 
        return x    
    


    